---
title: "INCLASS PROJECT"
author: "Ted"
format: html
editor: visual
---

```{r}
library(tidyverse)
#install.packages("readxl")
library(readxl)
```

## 

```{r}
library(readxl)
MSM2ndYear <- read_excel("MSM2ndYear.xlsx")
View(MSM2ndYear)
```

```{r}
ggplot(
  data = MSM2ndYear,
  mapping = aes(
    x = Age,
    y = FavMovie,
  )
) +
  geom_point()

```
